query userSession($token: String!) {
  userSession(token: $token) {
    email
    mongoId
    actual_points
    address
    state
    firstName
    lastName
    giftcard{
      id
      redemptionCode
    }
  }
}
